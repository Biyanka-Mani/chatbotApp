<div class="ui inverted large menu segment">


    <div class="ui inverted secondary pointing menu"> 
             <%= link_to 'ChatRoom',root_path,class:"item"%>
                <a class="item">
                Messages
            </a>
        </div>

        <div class="ui inverted secondary pointing menu right menu">
            <div class="ui dropdown item">
            Account <i class="dropdown icon"></i>
            <div class="menu">
                <%if logged_in?%>
                    <%= link_to 'Logout',logout_path,class:"item",data:{ 
                                turbo_method: :delete,
                                turbo_confirm: "Are you sure?"
                        } %>
                            
                <%else%>
                    <%= link_to 'Login',login_path,class:"item"%>
                    <a class="item">SignUp</a>
                <%end%>
            </div>
            </div>
            <%if logged_in?%>
                <div class="item">
                    
                      <%= link_to 'Logout',logout_path,class:"ui primary button",data:{ 
                                turbo_method: :delete,
                                turbo_confirm: "Are you sure?"
                            } %>
                </div>
                <%else%>
                <div class="item">
                     <%= link_to 'Login',login_path,class:"ui primary button"%>
                </div>
            <%end%>
        </div>
   </div>
 
</div>